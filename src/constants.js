
const DOUBLE_TAP_RADIUS = 25;

const NUM_HOLDERS = 3;

const MIN_SWIPE_DISTANCE = 30;

const DIRECTION_CHECK_OFFSET = 10; // amount of pixels to drag to determine direction of swipe

const ACCEPTABLE_CONTENT_FORMAT = [
  'src', // IMAGE_LINK
  'html', // HTML_STRING
  'dom' // DOM_NODE
];

export {
  DOUBLE_TAP_RADIUS,
  NUM_HOLDERS,
  MIN_SWIPE_DISTANCE,
  DIRECTION_CHECK_OFFSET,
  ACCEPTABLE_CONTENT_FORMAT
}
